import 'package:flutter/material.dart';

/// Placeholder — full implementation in Phase 7, Prompt 18.
class TimerSetupScreen extends StatelessWidget {
  const TimerSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Start Focus Session')),
      body: const Center(child: Text('Timer Setup — Phase 7')),
    );
  }
}
