import 'package:flutter/material.dart';

/// Placeholder — full implementation in Phase 2, Prompt 7.
class PermissionSetupScreen extends StatelessWidget {
  const PermissionSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Setup Permissions')),
      body: const Center(
        child: Text('Permission Setup — Phase 2'),
      ),
    );
  }
}
