import 'package:flutter/material.dart';

/// Placeholder — full implementation in Phase 9, Prompt 22.
class ArjunaChatScreen extends StatelessWidget {
  const ArjunaChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Arjuna — AI Coach')),
      body: const Center(child: Text('Arjuna Chat — Phase 9')),
    );
  }
}
